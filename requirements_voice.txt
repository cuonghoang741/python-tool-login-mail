# Voice Cloning & Synthesis dependencies
# Requires Python 3.9-3.11 (NOT 3.12+)
# Use with: venv_voice\Scripts\python.exe -m pip install -r requirements_voice.txt

PySide6>=6.5.0
torch>=2.0.0,<2.9.0
torchaudio<2.9.0
TTS>=0.20.0
numpy>=1.19.0,<2.0.0
soundfile>=0.12.0
# Fix for BeamSearchScorer compatibility issue - must use 4.36.x or earlier
transformers==4.36.2

# Japanese text processing dependencies for some voice models
# - cutlet: basic romaji/kana conversion
# - fugashi[unidic-lite] / mecab-python3 / unidic-lite: MeCab tokenizer + light dictionary
cutlet>=0.3.0
fugashi[unidic-lite]>=1.3.0
mecab-python3>=1.0.0
unidic-lite>=1.0.0


